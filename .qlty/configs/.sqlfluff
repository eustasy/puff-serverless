[sqlfluff]
# Single source of truth for the dialect: qlty.toml no longer pins --dialect on
# the command, so switch mysql/postgres/sqlite/… here with no other edits.
dialect = postgres
templater = raw
max_line_length = 140
# The schema is CockroachDB DDL (STRING, :::casts, LOCALITY ... REGIONAL BY
# TABLE, AS (...) STORED, PK column ASC). sqlfluff has no CockroachDB dialect and
# postgres — the closest — cannot parse these, so suppress parse (PRS) errors;
# sqlfluff still applies layout/capitalisation rules to the parts it can read.
ignore = parsing

[sqlfluff:indentation]
# indent_unit / tab_space_size are only honoured under [sqlfluff:indentation],
# NOT under [sqlfluff] (where sqlfluff silently ignores them). Default is 4
# spaces; a project that indents SQL with tabs sets `indent_unit = tab` here.
indent_unit = space
tab_space_size = 4
indented_joins = false
indented_using_on = true
template_blocks_indent = false

[sqlfluff:rules:capitalisation.keywords]
capitalisation_policy = upper

[sqlfluff:rules:capitalisation.identifiers]
capitalisation_policy = consistent

[sqlfluff:rules:capitalisation.functions]
capitalisation_policy = upper

[sqlfluff:rules:capitalisation.literals]
capitalisation_policy = upper
